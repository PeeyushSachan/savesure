import 'package:flutter/material.dart';

class CoupansCardTab extends StatelessWidget {
  const CoupansCardTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Copans page"),
    );
  }
}
